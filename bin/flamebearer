#!/usr/bin/env node
import fs from 'node:fs';
import {parseTrace, formatReport} from '../index.js';

const file = process.argv[2];
if (!file) {
    console.error('usage: flamebearer <trace.json[.gz]>');
    process.exit(1);
}

console.log(formatReport(parseTrace(fs.readFileSync(file))));
